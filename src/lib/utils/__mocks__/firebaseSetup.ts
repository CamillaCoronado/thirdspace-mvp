// This mock file prevents Firebase from initializing during tests
export const auth = {
  // Optionally, you can mock specific auth methods if needed
  // Example:
  // signInWithEmailAndPassword: jest.fn(() => Promise.resolve({ user: { uid: 'test-uid' } })),
};
